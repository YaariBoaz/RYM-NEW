import { AlertDetailsModalComponent } from './alert-details-modal.component'

describe('AlertDetailsModalComponent', () => {
  it('should mount', () => {
    cy.mount(AlertDetailsModalComponent)
  })
})